# Application Configuration
APP_ENV=development
APP_PORT=8080

# CSRF Protection Secret (generate a random 32-character string for production)
CSRF_SECRET=must-be-at-least-32-characters-long

# Session Key (generate a random 32-character string for production)
SESSION_KEY=super-secret-session-key-change-me

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=fiber_htmx_db
DB_SSLMODE=disable
